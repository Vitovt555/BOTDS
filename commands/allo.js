module.exports = {
    name: 'allo',
    execute(interaction) {
        interaction.reply('Sho nada?');
        // if(message.author.username === "CyberSlug") message.channel.send(":coin:");    
   }
}